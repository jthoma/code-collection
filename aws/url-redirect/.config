
# Only the bucket name 
BUCKET_NAME="[your bucket]"

# website domain http if direct s3 
# better use cloudfront also for free ssl and edge caching
SHORT_PREFIX="http(s)://hostname"

# Temp path for reading contents from an already shortened url
# Termux on Android $PREFIX/tmp/ linux can use '/dev/shm/'
TMPDIR="$PREFIX/tmp/"
